# harnesses/base

冻结基线 harness：**DSH base（固定 SHA）+ shopping-base plugin**。

- DSH 侧 SHA 见 `../../DEPENDENCIES.md`，源码只读。
- shopping-base 内容位于 `../../plugins/shopping/`。
- 本目录保存基线的装配描述/快照引用（后续实现）。

当前状态：占位，尚未实现。
