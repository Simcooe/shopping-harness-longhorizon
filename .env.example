# ShopSimulator 本地环境配置示例（复制为 .env 仅供参考；脚本不强制依赖它）。
# 只包含本地环境地址配置。
# ShopSimulator 不需要 API key；shopping environment adapter 也只读取
# SHOPSIM_BASE_URL，不读取任何模型配置或密钥。
SHOPSIM_BASE_URL=http://127.0.0.1:5700
SHOPSIM_PORT=5700
